max = []
        current = []
        for n in s:
            if not n in current:
                current.append(n)
                if len(max) < len(current):
                    max = current
            elif n in current :
                index_n = current.index(n)
                current = current[index_n +1:]
                current.append(n)
        return len(max)
