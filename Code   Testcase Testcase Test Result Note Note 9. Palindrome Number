class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        str_x = str(x)
        for index,n in enumerate(str_x):
            if str_x[index] == str_x[-(index + 1)]:
                continue
            else:
                return False
        return True
